# WebAssembly Text Catalog Adapter

This generated folder exposes the complete GRIMOIRE catalog to the `webassembly-wat` language target.

## Contents

- `algorithms.json`: 1000 catalog records with IDs, titles, nav labels, engine/domain facets, source status, and visual/sonic families.

## Boundary

This is a generated catalog adapter. It proves that WebAssembly Text has an addressable 1000-record input matrix for docs, generators, examples, and future native code batches. It does not claim that all 1000 algorithms have native WebAssembly Text implementations yet.
